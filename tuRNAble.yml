channels:
  - bioconda
  - conda-forge
  - defaults
dependencies:
  - flake8
  - nose
  - coverage >= 6  # to ensure lcov option is available
  - pandas
  - scikit-bio
  - tqdm
  - snakemake
  - pip:
     - "--editable=git+https://github.com/jlab/phylofiller.git"
